spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-twilight-shape-aabh6tvj-pooler.westus3.azure.neon.tech/neondb?sslmode=require&channelBinding=require}
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASS:npg_AaLhRiv04HTf}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace